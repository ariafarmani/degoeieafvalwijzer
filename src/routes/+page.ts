// Prerender the homepage as static HTML: fastest possible first paint and
// fully crawlable markup for Google. The calendar data is fetched client-side
// after the user searches, so nothing here depends on a server request.
export const prerender = true;
